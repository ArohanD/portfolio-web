import * as colors from './colors';
import { skillObject } from './index'

//TODO: Revisit color memoization

const getStandardColor = (skills: skillObject , colors) => {


  return () => {

  }
}

export default getStandardColor